# Non-secret function parameters (safe to commit) — Firebase reads this file at
# deploy time. Required so CI (non-interactive) deploys don't prompt for them.
# The Gemini API key is NOT here: it lives in Secret Manager as AI_API_KEY.
AI_PROVIDER=gemini
AI_MODEL=gemini-2.0-flash
