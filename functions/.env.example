# Copy to functions/.env before deploy (.env is gitignored).
# Provider for the Ask box. Recommended: gemini (cheap, same Google billing as Firebase).
AI_PROVIDER=gemini
# Optional model override. Blank = sensible default (gemini-flash-latest).
AI_MODEL=
